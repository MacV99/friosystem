<section id="stats">
  <div class="stats-inner">
    <div class="stat-item">
      <span class="stat-num">13<sup>+</sup></span>
      <span class="stat-lbl">Años de<br />Experiencia</span>
    </div>

    <div class="stat-divider"></div>

    <div class="stat-item">
      <span class="stat-num">4</span>
      <span class="stat-lbl">Sectores<br />Atendidos</span>
    </div>

    <div class="stat-divider"></div>

    <div class="stat-item">
      <span class="stat-num">6<sup>+</sup></span>
      <span class="stat-lbl">Marcas<br />Representadas</span>
    </div>

    <div class="stat-divider"></div>

    <div class="stat-item">
      <span class="stat-num">30<sup>+</sup></span>
      <span class="stat-lbl">Clientes<br />de Referencia</span>
    </div>
  </div>
</section>

<style>
  #stats {
    background-color: var(--clr-primary);
    padding: 0;
    gap: 0;
    align-items: center;
  }

  .stats-inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 960px;
    gap: 0;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 7px;
    padding: 36px 32px;
    flex: 1 1 140px;
  }

  .stat-num {
    font-size: 3rem;
    font-weight: 800;
    color: #fff;
    line-height: 1;
    letter-spacing: 1px;
  }

  .stat-num sup {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--clr-highlight);
    vertical-align: super;
  }

  .stat-lbl {
    font-size: 0.68rem;
    color: rgba(255, 255, 255, 0.55);
    text-transform: uppercase;
    letter-spacing: 2px;
    text-align: center;
    line-height: 1.6;
    font-weight: 600;
  }

  .stat-divider {
    width: 1px;
    height: 48px;
    background: rgba(255, 255, 255, 0.12);
    display: none;
    flex-shrink: 0;
  }

  @media (min-width: 640px) {
    .stat-divider {
      display: block;
    }

    .stat-num {
      font-size: 3.5rem;
    }
  }
</style>
