<section class="hero flex-column">
  <div class="hero-particles" aria-hidden="true">
    <i class="bi bi-snow p1"></i>
    <i class="bi bi-snow2 p2"></i>
    <i class="bi bi-snow p3"></i>
    <i class="bi bi-snow2 p4"></i>
    <i class="bi bi-snow p5"></i>
    <i class="bi bi-snow2 p6"></i>
    <i class="bi bi-snow p7"></i>
    <i class="bi bi-snow2 p8"></i>
    <i class="bi bi-snow p9"></i>
    <i class="bi bi-snow2 p10"></i>
    <i class="bi bi-snow p11"></i>
    <i class="bi bi-snow2 p12"></i>
  </div>

  <div class="hero-content flex-column">
    <span class="hero-eyebrow">Neiva, Huila · Colombia</span>
    <h1>AC FRIOSYSTEM S.A.S</h1>
    <p class="hero-sub">Soluciones en climatización y aire acondicionado</p>

    <div class="hero-pills">
      <span><i class="bi bi-tools"></i> Mantenimiento</span>
      <span><i class="bi bi-rulers"></i> Diseño y Montaje</span>
      <span><i class="bi bi-box-seam"></i> Suministro</span>
    </div>

    <div class="hero-btns">
      <a
        class="boton hero-btn"
        href="https://wa.me/573107751017?text=Hola%2C%20quiero%20más%20información%20sobre%20sus%20servicios"
        target="_blank"
        rel="noopener noreferrer"
      >
        <i class="bi bi-whatsapp"></i>
        CONTÁCTANOS
      </a>
      <a class="hero-btn-outline" href="#servicios">
        Ver servicios
        <i class="bi bi-arrow-down"></i>
      </a>
    </div>
  </div>

  <a href="#nosotros" class="hero-scroll" aria-label="Ir al contenido">
    <i class="bi bi-chevron-down"></i>
  </a>
</section>

<style>
  .hero {
    position: relative;
    text-align: center;
    background-image: url("/img/hero.jpg");
    background-attachment: fixed;
    background-repeat: no-repeat;
    background-position: center center;
    background-blend-mode: color;
    background-color: var(--hero-overlay);
    background-size: cover;
    height: 520px;
    overflow: hidden;
    gap: 0;
    justify-content: center;
  }

  .hero-content {
    gap: 14px;
    z-index: 2;
    animation: fadeInUp 0.8s ease both;
    padding: 0 20px;
  }

  .hero-eyebrow {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--clr-red);
    animation: fadeInUp 0.8s 0s ease both;
    display: block;
  }

  h1 {
    font-size: 24px;
    line-height: 1.2;
    color: var(--clr-white);
    letter-spacing: 6px;
    padding-left: 6px;
    text-shadow: 0 2px 24px rgba(0, 0, 0, 0.5);
  }

  .hero-sub {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.78rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: 500;
    animation: fadeInUp 0.8s 0.15s ease both;
    max-width: 420px;
    line-height: 1.7;
  }

  /* ─── PILLS ─── */
  .hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    justify-content: center;
    animation: fadeInUp 0.8s 0.25s ease both;
  }

  .hero-pills span {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    color: rgba(255, 255, 255, 0.85);
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 1.5px;
    padding: 6px 14px;
    text-transform: uppercase;
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
  }

  .hero-pills span i {
    font-size: 12px;
    display: inline-flex;
    color: var(--clr-red);
  }

  /* ─── BUTTONS ─── */
  .hero-btns {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    justify-content: center;
    animation: fadeInUp 0.8s 0.35s ease both;
    margin-top: 6px;
  }

  .hero-btn {
    background-color: var(--clr-white);
    color: var(--clr-blue);
    font-weight: 700;
    letter-spacing: 2px;
    padding: 12px 24px;
    border-radius: 50px;
    font-size: 13px;
    box-shadow: var(--shadow-md);
    transition: all 0.25s ease;
    display: inline-flex;
    align-items: center;
    gap: 7px;
  }

  .hero-btn-outline {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    background: transparent;
    border: 1.5px solid rgba(255, 255, 255, 0.45);
    border-radius: 50px;
    color: rgba(255, 255, 255, 0.9);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.5px;
    padding: 11px 22px;
    transition: all 0.25s ease;
  }

  .hero-btn-outline i {
    font-size: 14px;
    display: inline-flex;
  }

  /* ─── SCROLL INDICATOR ─── */
  .hero-scroll {
    position: absolute;
    bottom: 24px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    color: rgba(255, 255, 255, 0.5);
    font-size: 20px;
    display: flex;
    animation: scrollBounce 2s ease infinite;
    transition: color 0.2s;
  }

  @keyframes scrollBounce {
    0%,
    100% {
      transform: translateX(-50%) translateY(0);
    }
    50% {
      transform: translateX(-50%) translateY(6px);
    }
  }

  /* ── PARTÍCULAS ── */
  .hero-particles {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    max-width: none;
    width: 100%;
  }

  /* Base: solo posición y color — animación va en cada .pN */
  .hero-particles i {
    position: absolute;
    color: rgba(255, 255, 255, 0.38);
  }

  /* Cada partícula tiene su propia duración y delay dentro del shorthand */
  .p1 {
    font-size: 30px;
    top: 12%;
    left: 3%;
    animation: windDrift 4.5s 0s linear infinite;
  }
  .p2 {
    font-size: 20px;
    top: 65%;
    left: 10%;
    animation: windDrift 5.2s 1.2s linear infinite;
  }
  .p3 {
    font-size: 16px;
    top: 28%;
    left: 20%;
    animation: windDrift 3.8s 0.5s linear infinite;
  }
  .p4 {
    font-size: 24px;
    top: 75%;
    left: 30%;
    animation: windDrift 4.8s 2.1s linear infinite;
  }
  .p5 {
    font-size: 18px;
    top: 18%;
    left: 42%;
    animation: windDrift 5.5s 0.8s linear infinite;
  }
  .p6 {
    font-size: 26px;
    top: 58%;
    left: 52%;
    animation: windDrift 4.2s 1.7s linear infinite;
  }
  .p7 {
    font-size: 22px;
    top: 35%;
    left: 63%;
    animation: windDrift 4.9s 0.3s linear infinite;
  }
  .p8 {
    font-size: 14px;
    top: 80%;
    left: 72%;
    animation: windDrift 3.6s 2.5s linear infinite;
  }
  .p9 {
    font-size: 28px;
    top: 20%;
    left: 81%;
    animation: windDrift 5.1s 1s linear infinite;
  }
  .p10 {
    font-size: 18px;
    top: 60%;
    left: 88%;
    animation: windDrift 4.4s 1.8s linear infinite;
  }
  .p11 {
    font-size: 22px;
    top: 40%;
    left: 93%;
    animation: windDrift 5s 0.6s linear infinite;
  }
  .p12 {
    font-size: 16px;
    top: 82%;
    left: 97%;
    animation: windDrift 3.9s 2.8s linear infinite;
  }

  @keyframes windDrift {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg);
    }
    12% {
      opacity: 0.55;
    }
    85% {
      opacity: 0.45;
    }
    100% {
      opacity: 0;
      transform: translate(40px, -55px) rotate(28deg);
    }
  }

  /* ─── HOVER ─── */
  @media (hover: hover) {
    .hero-btn:hover {
      background-color: var(--clr-blue);
      color: var(--clr-white);
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }

    .hero-btn-outline:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: rgba(255, 255, 255, 0.7);
      color: #fff;
    }

    .hero-scroll:hover {
      color: rgba(255, 255, 255, 0.9);
    }
  }

  /* ─── RESPONSIVE ─── */
  @media (min-width: 750px) {
    h1 {
      font-size: 36px;
      letter-spacing: 10px;
    }

    .hero-sub {
      font-size: 0.85rem;
      max-width: 520px;
    }
  }
</style>
